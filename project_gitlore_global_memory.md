---
name: gitlore-global-memory-investigation
description: "N-tier global/org memory: tiers as nested submodules, agent-picks-dir routing, activation via the `memory/.gitlore-tiers` manifest (FR15/D17), plus active recall from a mid-task trigger (FR16/D18). Read it before touching tier mounting, composition, the `live` branch model, tier lockstep, `/gitlore:add-tier`, the recall request file, or the eval scenarios that cover them — it carries current slice status, the settled decisions (index one-liner canonical for a line's TEXT and, since 2026-07-22, authoritative over its PRESENCE too — non-destructively, so removing a line never deletes the file, which closes coverage/prune/dedup; detached-at-live; tier remotes default to `main` with `live` alongside; one merge policy at every level — memory and each tier get the same two gates and the same `/gitlore:resolve`, with the store carried in the merge-state file) and the open ones (index size, the unbuilt dangling-pointer report)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7affbb95-e26e-4294-8835-9c686a979955
  modified: 2026-07-22T17:45:55.532Z
---

Investigating whether gitlore should support a GLOBAL/organizational memory tier alongside its per-project memory. Motivation: David has ~16 sibling repos each with a gitlore `memory/` submodule; `user` + CC-platform `reference` + portable `feedback` memories are trapped and duplicated per-repo, while `project` memories are correctly per-repo.

**Conclusion so far:** a flat merge-everything store is WRONG — it blows the ~200-line always-loaded `MEMORY.md` budget and loads 15 irrelevant projects every session. Right shape is **two-tier**: a global tier (user + CC-platform reference + portable feedback) loaded everywhere, plus per-project tiers staying local. This aligns with a structure Anthropic ALREADY ships — the `team/` subdir + combined index (see [[cc-memory-retrieval-agentic]]). **Design principle: keep Anthropic's memory structure, upgrade only gitlore's GENERATION step — that structure is Anthropic's surface.**

**Load-bearing facts.** Retrieval is agentic (≤5 selective attach), so a large global tier costs little context IF surfaced on-selection rather than bulk-loaded. gitlore today: flat memory submodule, hand-edited `MEMORY.md` (NO generation step anywhere), write path is a uniform `git add -A` (`scripts/lib/resolve.sh:158-161`) with ZERO per-file/per-type routing. `docs/design.md:637` already rejected a global `userSettings` `autoMemoryDirectory` (all projects → one repo). NOTE: the early "write-routing = a classifier" framing was SUPERSEDED during design — routing is the agent picking a directory (see converged design below), no content classifier.

**Experiment DONE (2026-07-14) — both unknowns resolved, the clean two-tier design is confirmed viable** (probe details in [[cc-memory-retrieval-agentic]]; protocol was `~/.claude/plans/go-precious-crystal.md`, sentinel-introspection via `claude --print --settings autoMemoryDirectory=<scratch>`, no real-memory contact):
- Retrieval attaches file BODIES on-selection under `--print` (mechanism fires; not "never read").
- **U1:** both the `MEMORY.md` one-liner AND the per-file `description:` frontmatter feed the classifier — but the index line is the CANONICAL one (always-loaded + the reliable lever); see DRIFT FINDING below.
- **U2a:** a `team/`-style subdir IS enumerated natively (100% for indexed subdir files); depth ≥2 works too.
- **U2b:** subdir/`team/` bodies do NOT bulk-load — the always-loaded footprint is the `MEMORY.md` index blob only. A global tier of N files costs N index one-liners, NOT N bodies. Near-free at baseline as long as the index stays under ~200 lines.
- **Reliability:** indexed files retrieve 100%; an unindexed pure-frontmatter file retrieved 75%. → the generation step MUST emit `MEMORY.md` pointers for global files, not lean on frontmatter enumeration alone.

**CONVERGED DESIGN — captured as D17 in `docs/design.md` (design only; not yet implemented or planned).** Generalized from "two-tier" to **N named tiers**:
- **Materialization:** each tier (`memory/lore` global, `memory/ddaanet` org, …; names configurable) is a **nested git submodule** inside the project memory submodule, reusing the existing init / FR11 gate / push-lockstep framework. `autoMemoryDirectory` is single-path, so tier content is physically present as nested checkouts under it.
- **Routing = agent picks the directory**, guided by an addition to the standing SessionStart `additionalContext` (D12 block): portable → `memory/<tier>/`, project → `memory/`. No content classifier — the directory *is* the tier *is* the submodule; the FR11 gate commits whichever submodule the file landed in. (Routing guidance rides `additionalContext` → may not inject under `--print` per D14; a misfile is self-correctable, never corruption.)
- **Index composition is PLACEMENT ONLY, not frontmatter-derived and not hygiene** (CORRECTED 2026-07-14; scope narrowed 2026-07-17). `SessionStart` splices each mounted tier's `memory/<tier>/MEMORY.md` carrier into the root index as a block (global first, project bare-path last), removes a block for a tier no longer mounted, and mirrors root tier-lines back into the carrier so they propagate. That is the whole job. The agent (and CC native pointer-writing) owns each line's TEXT plus its per-file creation/deletion; composition owns only tier-block placement. Writers disjoint by ASPECT (text+per-file-presence vs tier-block-placement). Coverage/prune/dedup are REJECTED OUTRIGHT (see the 2026-07-22 presence-authority block below) — the first two contradict or invert index-authority-over-presence, the third guards a non-existent union driver.
- **Authoring-time ONE-WAY sync (index → frontmatter)** (`PreToolUse`+`PostToolUse(Write|Edit)`, CORRECTED 2026-07-15 — was "bidirectional"): mirrors an edited index one-liner's hook → frontmatter `description` (after-the-dash hook only, never `name`/title). One-way because the index is canonical — a bidi sync resolves conflicts by tool-call order (last writer wins), and since the file body is usually written after the index, the weaker frontmatter would propagate back and clobber the curated index hook (store proof: index hooks carry content frontmatter lacks — `feedback_memory_before_root_commit`, `reference_cc_worktree_memory_freeze`). `PreToolUse` captures the pre-edit index image so PostToolUse propagates ONLY the changed line(s), never a blanket sweep (which would push a stale index line onto fresh frontmatter). Frontmatter-only edits untouched; hook never writes the index. Deploys globally via plugin hooks (free per-project); complementary to the structural pass. REFINED 2026-07-16 (settled): the diff is keyed by DESTINATION path (so reordering / grouped mid-list insertion is a no-op); an ADDED line fills frontmatter only if the description is EMPTY (no overwrite — by write-order a real creation already carries its authored description, so this is a no-op there and no file-added check is needed); a CHANGED hook propagates (overwrites). See the RESOLVED block below.
- **One-time reconcile** — the semantic complement the one-way sync CAN'T do: it never fixes a stale INDEX line (harmful direction, `reference_git_hook_env_leak`), because fresher-of-two is a semantic call. Runs AFTER sync deployment (else editing re-drifts frontmatter). Per-project + once-per-shared-tier, opportunistic/lazy (optional `/gitlore:reconcile`, dogfood here), worth an explicit sweep only where a stale INDEX line bites; stale frontmatter is low-harm.

**DRIFT FINDING (2026-07-14 — refutes the earlier "frontmatter = single source of truth").** Git-history audit of THIS repo's memory: the root `MEMORY.md` one-liner and the frontmatter `description` drift BIDIRECTIONALLY — the agent only has the always-loaded root index in context, so it revises whichever surface it sees and the other rots, in *both* directions (`feedback_memory_retrieval_in_practice` = fresh index / stale frontmatter; `reference_git_hook_env_leak` = the reverse: corrected frontmatter+body, stale index line). So neither is a reliable SoT; picking the fresher of two divergent texts is a semantic call. The **index one-liner is canonical** (always-loaded + reliable retrieval lever, 100% vs 75%); frontmatter is a weaker secondary match-surface. This is *why* composition must be structural (above), not text-derivation.
- **Propagation + pinning:** SessionStart ff's the nested tier submodule(s) so cross-project global facts arrive; the recompose leaves `memory/` DIRTY — expected — and rides the next parent commit (float, but commits naturally; no scheduled sync-commit, no gate churn).
- **Conflicts:** concurrent tier-index insertions merge via the semantic memory-merger sub-agent (three-way, no diff3) — no duplication. NO `merge=union` driver (it concatenates blindly, manufacturing the duplicates a dedup would then clean). Distinct per-index namespaces bar cross-index collision. No append-only constraint; conflicts rare (more global → more stable).

**Empirical basis** for "compose into root index": interactive PTY + `--print` both confirm recall = tool-gated `Read` steered by the ROOT `MEMORY.md`; nested `team/MEMORY.md` NOT auto-loaded; index-listed → ~100% recall, unindexed → ~75%. See [[cc-memory-retrieval-agentic]].

**RESOLVED 2026-07-16 — D17 SPOT settled by eval (MemoryEval sub-agent; 528 session transcripts + a 136-file native baseline across 7 OTHER projects where gitlore's sync hook has not yet acted).** The OPEN QUESTION was: does "index one-liner canonical" cost more in AUTHORING than it wins in RETRIEVAL? Answer: **index-canonical holds, and the authoring cost dissolves once the sync stops overwriting at creation.**
- **V1 — agents curate the index hook, not the frontmatter description (≈3:1).** Post-creation Edit tool calls that actually changed a value: **40 index-hook refinements vs 14 description edits** (18 distinct files vs 7). Holds OTHERS-ONLY at 3.1:1, so it is not a gitlore artifact — it is how native auto-memory gets curated. The description is the summary agents touch *least* after birth. Confirms index-canonical on the AUTHORING side too, not just retrieval.
- **V2 — the "hook authors the index + notifies via additionalContext" reframe FAILS its precondition.** A new index line is NOT reliably end-appended: flat gitlore END=12 / MID=4 (~25% inserted mid-list, always grouped by type prefix), and sectioned indexes in other projects (candidature, edify) put the pointer list before other sections so an end-append lands in the wrong section entirely. Layout-aware placement is exactly the judgment the agent already applies → the reframe doesn't remove the agent from index authoring, only relocates the hard part.
- **Descriptions are meaningful at creation; slug descriptions do not occur.** 0/136 native descriptions are slug-like — the only real gap is the occasional MISSING description, handled by fill-if-empty. So slug-detection guards a non-existent case → REJECTED.
- **Settled sync rule (keyed by DESTINATION path, not line position):** (1) line ADDED → fill that file's `description:` only if empty, else leave it; (2) hook CHANGED for an existing destination → overwrite/propagate to the description; (3) unchanged / reordered / removed → no-op. Never a blanket sweep; never clobber a non-empty description at add-time; the write-order fact (file-with-description first, index line second) makes fill-if-empty a no-op on real creations, so NO separate file-added check is needed. REJECTED: bidirectional sync (write-order makes the index the last write, so bidi lets the weaker side win by tool-call order); slug-detection; the append/additionalContext reframe (V2).
- **Load-bearing follow-up — DONE 2026-07-16 (TDD).** The live slice-1 post-hook loop (in `scripts/cc-hooks/index-sync-post.sh`, using helpers from `scripts/lib/index-sync.sh`) previously OVERWROTE on an added line — a destination absent from the pre-image made the "unchanged?" test fail and it wrote unconditionally. Now the loop is keyed by presence-in-pre-image: an existing line propagates only when its hook changed; an **added** line (absent from the pre-image) fills `description` only when `gitlore_get_frontmatter_description` returns empty, never clobbering authored prose. 2 new `index_sync.bats` cases (added-line clobber-protection + empty-fill); `make test` 262/0. Verified LIVE this session — editing these two MEMORY.md lines fired the changed-line propagate path (additionalContext confirmed the frontmatter rewrite).

**Slices 1-2 COMPLETE** (one-way sync + reconcile — see the log above). **Slice 3a BUILT AND REVERTED 2026-07-17** on eyeball review. The reverted `scripts/lib/index-recompose.sh` carried dedup-by-path + prune-orphans + coverage-seed, `SessionStart`-only. Why reverted: (1) **dedup** guarded residue from a `merge=union` driver that is not used and won't be — distinct per-index namespaces make cross-index duplication impossible, and concurrent tier insertions merge through the semantic memory-merger without duplicating; (2) **prune** and **coverage** are PREMATURE, not wrong: they presuppose that the file set is authoritative over line PRESENCE — an OPEN question (index is canonical for line TEXT per the SPOT eval; presence is a separate axis, needs a log analysis of how presence actually drifts, and David's hunch leans FILE-SET-authoritative, which would favour them). Baking either direction into a hook now decides by default what should be decided later on evidence. Each also silently hardcodes a SEMANTIC call that belongs to the agent (prune can destroy the last recoverable record of a lost memory → should report, not drop; coverage can resurrect a deliberately-removed line → net-new pointers are already written by agent/CC on save); (3) none advanced nested memory — the tier machinery was already deferred. When the presence-authority question is taken up, coverage/prune return to the table on their merits. Reverted the lib + its 11-case bats suite + the `session-start.sh` wiring/test + the plan doc; folded reasoning into design.md composition/Conflicts paragraphs + two new Rejected Alternatives rows.

**SLICE 3 FULLY DESIGNED 2026-07-18 — free-form multi-tier; authoritative detail is design.md D17 (materialization / routing / composition / manifest / triggers / add-create / branch-model paragraphs + status). Load-bearing decisions this session:**
- **Discovery by ENCLOSURE, no hardcoded tier name.** Every submodule in the memory store's OWN `memory/.gitmodules` is a tier — `SessionStart` enumerates them. Asymmetry with the parent, which picks ONE submodule by the fixed name `gitlore-memory` (`GITLORE_SUBMODULE_NAME`); at the memory level the enclosure is the signal, so "don't hardcode the tier name" falls out for free. First concrete tier is org-scoped **`ddaanet`** (no global `lore` yet — nobody to share with).
- **Line→tier identity by PATH PREFIX, no sentinel text in either index.** A root pointer whose path starts with an active tier dir (`ddaanet/…`) is that tier's line; bare path = local/project line. Composition splices carrier lines UP prefix-added and mirrors root tier lines DOWN prefix-stripped (so they travel). Composed order: `[tier blocks, manifest order] → [project lines last]`; idempotent.
- **Routing guidance SELF-DESCRIBES on the tier** (its `MEMORY.md` frontmatter `description:`), travels to every consumer, reported at SessionStart for ACTIVE tiers only. Chosen over a per-repo descriptions manifest because duplicating routing text per repo is the drift this project kills.
- **Ordering + activation = an explicit CONSUMER-LOCAL manifest `memory/.gitlore-tiers`** (tracked, one tier path per line, top = highest precedence). *Listed = active*; a mounted-but-unlisted tier is dormant (not composed, not advertised). NOT `.gitmodules` (incidental add-order, git-porcelain-rewritten) and NOT index marker text. Deliberately edited (never auto-populated) so a half-formed tier stays invisible. Multi-tier ordering was the one genuinely-missing piece; `project`-interleave token deferred (default project last).
- **Mount vs CREATE, manifest edited LAST.** Create (rare, first repo stands up a tier): init empty module → seed MEMORY.md+frontmatter → create+push remote → *then* list. The "module exists" before "module self-describes" gap is harmless precisely because it is not yet listed.
- **Compose runs mid-session on `PostToolUse` when `.gitlore-tiers` changes** (magic-file trigger; SessionStart-only ruled out by the agent-driven add-tier validate-and-reorder story), FAIL-SAFE: refuses (systemMessage+additionalContext, no clobber) on a duplicate pointer line or a manifest entry for an ABSENT module. Present-but-unlisted = dormant (not an error).
- **BRANCH MODEL UNIFIED to detached-at-`live` — memory AND tiers, one commit path.** The per-parent-branch name was only a local checkout handle (never travelled, gave no branch-aligned history since memory ff-pushes `HEAD:live` every commit), so dropping it costs nothing semantic and collapses the `branch-vs-live`/`local-vs-remote` split into "my pending commit vs authoritative `live`, re-detach at new live." Refactor of `session-start.sh`/`sync_memory_to_live`/`resolve.sh` — no compat tax (sole user). Memory kept named branches ONLY out of "don't refactor working code," which sole-user neutralizes.
- **SLICE SEQUENCE:** **3-i-a** (materialize + `SessionStart` detach-at-`live` ff propagation-in + self-describing routing — no commit path; plan `docs/plans/2026-07-18-11-tiered-memory-3i-propagation.md`, TDD bats, spike-first for nested-submodule mechanics) → **branch-model unification** (Plan-03-level refactor) → **tier commit/push lockstep** (rides unified path) → **3-ii** composition internals → **3-iii** `/gitlore:add-tier` (mount + `--create`). Coverage/prune/dedup STILL deferred behind the unsettled presence-authority question.

**3-i-a CODE DONE 2026-07-19 (Tasks 1-3, TDD, `make test` 291/0).** `gitlore_tier_paths` + `gitlore_active_tiers` in `scripts/lib/util.sh`; a guarded `SessionStart` block that materializes each tier, ff's it with `fetch origin live:live`, detaches at `live`, then appends active-tier routing guidance (each tier's own frontmatter `description:`) to the standing `additionalContext`. Fixtures `tests/helpers/tier-fixtures.bash`, 13 cases in `tests/tier_discovery.bats`. Nested-submodule git mechanics recorded in [[nested-submodule-tier-mechanics]].

**TASK 4 DOGFOOD — TIER MOUNTED 2026-07-20.** `memory/ddaanet` → **private** `ddaanet/ddaanet-memory` (private departs from `gitlore_parent_visibility`, which says PUBLIC here: a tier aggregates facts from private projects, so it takes the stricter of its consumers'). `ddaanet` listed in `memory/.gitlore-tiers`. Three things this pinned that the fixtures could not: (1) the tier remote's **default branch must be `main`, with `live` alongside** — a mount checks out the default branch, so a `live` default gets checked out AS a branch and `fetch origin live:live` then refuses to fetch into the current branch, killing ff-only propagation-in; (2) **mounting needs NO commit inside the memory store** — `gitlore_tier_paths` reads `memory/.gitmodules` from the working tree, not from a commit, so the mount can sit staged and the FR11 gate stays the sole committer; (3) the manifest's "consumer-local" wording means **per-consuming-repo and tracked**, not per-machine — activation and precedence travel with the memory store to every clone (David's call, 2026-07-20). Agent cannot run the submodule git writes (strict sandbox + classifier) — they went through David's `!` shell via a prepared script. SessionStart fetch/detach + routing-guidance emission confirm on the next fresh session.

**3-i-a CONFIRMED LIVE 2026-07-20.** On a fresh session the SessionStart tier block emitted `memory/ddaanet` with the tier's own frontmatter `description:` as routing guidance, and the tier checkout is detached at `4eefd91` with local `live` == `origin/live` == `main` — so discovery-by-enclosure, the ff-only `fetch origin live:live`, and detach-at-`live` all work against the real mount. Slice 3-i-a is done.

**BRANCH-MODEL UNIFICATION DONE 2026-07-20 (memory side), 273+20 green.** Shapes worth knowing before touching this code again: one `gitlore_prepare_merge MEMPATH AUTHORITY` serves both flavors — pin HEAD at `refs/gitlore/pending`, `checkout --detach <authority>`, merge the pending commit in; detaching *at the authority* is what preserves D6's first-parent direction, and because no branch is ever checked out the concurrent-checkout failure is gone rather than handled. `refs/gitlore/pending` is load-bearing: after `merge --abort` nothing else references the divergent commit (the retired named branch did). One `continue-after-merge` continuation reads the flavor only to decide whether the `origin` push follows the local one; flavors are `head-vs-live`/`head-vs-remote` and `return_branch` left the state file. The remote flavor no longer does `reset --hard origin/live` — the merge carries local `live` forward. Test fixtures now advance `live` with plumbing (`advance_branch_with_file`), since it can no longer be checked out to commit on.

**TIER LOCKSTEP DONE 2026-07-21, 342+31 green** — and it settled both questions it was named as forcing. **One approval summary per memory EPISODE**, reused verbatim as the commit message in every store the episode touched: the user approves a set of writes, not a set of repositories, so the only thing the split was buying is grouping the approval prompt *by destination* (a tier line is more public than a project one). **No recursing pre-commit/pre-push in the memory store** — recursion is driver-side (`gitlore_sync_tiers_to_live` inside `gitlore_sync_memory_to_live`, tier pushes inside the parent `pre-push`), exactly as the parent already drives memory; hook-side would re-litigate the `--local-env-vars` unset and the `GIT_INDEX_FILE` capture a level down and force the FR11 gate to share a hook with the driver. The FR11 gate IS emitted into each tier (its own `gitlore.hooksDir` mirror, since the wrapper's `git config` reads the store it fires in). Ordering is load-bearing: tiers commit *before* memory's `add -A`, tiers push *before* memory's. Scope is every MOUNTED tier, not the manifest-active ones — dormancy governs routing, and dropping a dormant tier's writes is data loss. Every tier loop guards `[ -e "$tierpath/.git" ]` first (see [[reference_submodule_escape_to_parent]]): unguarded, `git -C` into an unchecked-out tier commits MEMORY under the tier's name and pushes memory's `live` to the tier's remote. Tier divergence is *detected* at the remote push and named, but not resolvable yet — `load_continuation_state` derives its store from `gitlore_memory_path`, so the state file would have to carry the store path for a tier merge to be threadable.

**3-ii composition DESIGNED + PLANNED 2026-07-21** (spec `docs/superpowers/specs/2026-07-21-tier-index-composition-design.md`, plan `docs/plans/2026-07-21-12-tiered-memory-3ii-composition.md`, four tasks). Settled beyond the D17 text: compose runs on **PostToolBatch** (not PostToolUse — one invocation, one message, all edits in the batch) plus SessionStart, and NOT in the pre-commit path (the only uncomposed route left is a `/gitlore:resolve` merge — recorded as a follow-up). **Mirror-down runs for every MOUNTED tier, splice-up only for ACTIVE ones** — D17's "a deactivated tier's lines persist in the carrier" only holds if they were mirrored while active, so a root line for a mounted-but-never-active tier would be silently dropped; same data-loss argument the lockstep used. **Root hook text wins** over a divergent carrier hook (root is canonical for a line's *text*). A **fourth validation** joins D17's three: a non-blank non-bullet line inside the bullet region refuses the pass, since the layout rule would relocate it. Mirror-down is **unconditional** (no carrier-file-exists check) precisely to avoid deciding presence-authority by the back door. Composition needs **no change to index-sync**: `index-sync-post.sh` already resolves `$mempath/$path`, so a prefixed path lands on the tier file, and a reorder is already a sync no-op. Routing guidance flips to "add the line to the ROOT index with a `<tier>/` prefix" — the surface the agent actually has loaded.

3-ii composition **BUILT 2026-07-22** (`scripts/lib/index-compose.sh` + `scripts/cc-hooks/index-compose.sh` on PostToolBatch, registered BEFORE memory-commit-batch so composed indexes are what commits; `session-start.sh` composes after the tier ff and routing guidance flipped to the root-prefixed surface; 40 new bats cases, full unit suite + `just precommit` green). Two spec refinements the build forced: mirror-down runs for every MOUNTED tier but splice-up only for ACTIVE ones (a root line for a mounted-but-never-active tier would else be silently dropped — same data-loss argument as the commit lockstep); the root's hook text wins over a divergent carrier hook (index canonical for text). A 4th validation beyond the design's three: a non-blank non-bullet line inside the bullet region refuses the pass. Dogfooded live — the ddaanet `reference_sandbox_git_lockfile` and `reference_bats_bang_no_fail` bullets now surface prefixed in the root index and mirror down into the carrier; byte-idempotent on re-run. NOTE the new hook is NOT active in the session that built it (CC freezes hook event-registration at session start), so composition was run by hand; it self-triggers next session.

**ACTIVE RECALL (FR16/D18) BUILT + DOGFOODED LIVE 2026-07-22** (`skills/recall/SKILL.md`, `scripts/lib/recall.sh`, `scripts/cc-hooks/recall-batch.sh` on PostToolBatch, `recall-reset.sh` on SessionStart+PreCompact; 28 cases, 410 green). The agent writes `.claude/gitlore-recall` (gitignored, one memory-relative path per line, or the literal `no match`) and the hook reads the bodies and injects them as `additionalContext` — no hook output can force a Read, so the hook does the reading, which is also one round trip instead of two. Four paths confirmed against the real store on a fresh session: mid-batch trigger → bodies inline; one-shot consumption (the request file is removed whether served or refused); ledger dedup (a re-request named the two already-read entries as skipped and fetched only the new one); refusal naming the unresolved path. The ledger lives in the memory gitdir keyed by session, records `<hash> <relpath>` so an edited memory re-fetches, and is fed from `.tool_calls[]` Reads so it also catches CC's OWN native recall. The `PreCompact` reset is **VERIFIED 2026-07-22** on the next real compaction, both ways: the ledger file was gone from the memory gitdir afterwards, and a body that had been fetched *before* the compaction came back as a fresh fetch rather than an "already in this context" skip. Native recall landing in the ledger is still unexercised (no "Recalled N memories" has fired). Cosmetic defect seen live: the refusal text says "nothing was read" twice and starts a sentence lowercase; unfixed because the exact string is likely asserted in the bats suite.

**3-iii `/gitlore:add-tier` DONE 2026-07-22** (`scripts/add-tier.sh`, `scripts/cc-hooks/add-tier-batch.sh` on PostToolBatch, `commands/add-tier.md`; 35 cases). The agent writes a one-shot `key=value` intent file `.claude/gitlore-add-tier` (`mode=mount|create`, `name`, `url`, `description`) and the hook runs the git. The trigger-file route was already the FR11 pattern, but a **second independent reason** forced it here: mounting a tier *clones*, and the agent's command sandbox has no network — satisfying the classifier alone would not have been enough. `mode=create` seeds the tier `MEMORY.md` (frontmatter `description:` = routing guidance), pushes `main` **then** `live` so the remote default settles on `main`, then takes the identical mount path. Both modes end MOUNTED-but-INACTIVE and make no commit inside memory; activation stays the deliberate manifest edit, which retriggers 3-ii composition. A tier name with whitespace is refused up front — it would mount fine but the line-oriented manifest could never list it.

**HAPPY-PATH EVALS DONE 2026-07-22** ([[feedback_evals_happy_path]]) — three new scenarios, all passing live at `EVAL_K=1`: `03-add-tier` (intent file → hook mounts → manifest edit recomposes the root index), `04-tier-write` (a portable fact routed into the tier, mirrored down, memory *and* tier committed from one approved summary), `05-recall` (request file → injected body → the answer uses it). The harness had to generalize first: scenarios now name an assertion script in `tests/evals/asserts/` and an optional fixture in `tests/evals/setups/`, `approval_message` is optional (a flow with no approval gate is one turn), and `{{EVAL_REPO}}` expands at trial time so a scenario can name a URL its fixture just created.

Three things that run deeper than the scenarios:

- **The eval repo's hooks are now derived from the plugin's real `hooks/hooks.json`**, not hand-listed. The old hand-written subset was `PostToolUse` alone — so composition, the commit gate, recall and add-tier were dark in *every eval ever run*. Exactly the failure [[feedback_test_the_invocation_path]] names. Probed first: `SessionStart`, `PreToolUse`, `PostToolUse` and `PostToolBatch` all fire under `claude --print` ([[feedback_posttooluse_print_mode]]).
- **The recall assertion reads the session transcript**, because every repo-visible signal is identical whether the body arrived by injection or by the agent simply Reading the file — same ledger record, same answer. Only the tool calls tell them apart, and that distinction is the whole mechanism. Located by session id, not by deriving the projects-dir mangling ([[reference_cc_project_dir_encoding]]).
- **The assertions are themselves tested** (`tests/evals/lib/asserts.bats`, plus `dispatch.bats` for the scenario loop): an assertion that always passes turns its scenario into an expensive no-op and the run still looks green.

This run also **dogfooded `add-tier-batch.sh` for the first time** — its `PostToolBatch` registration was frozen at session start in the session that built it, so it had never fired ([[reference_cc_hook_reload]]). It mounted a tier against a local bare remote; a real network remote is still unexercised. Fixtures reach no network at all, which is why `protocol.file.allow` has to be loosened through the environment ([[reference_git_protocol_file_allow]]).

**PRESENCE-AUTHORITY SETTLED 2026-07-22 (David's call) — the INDEX is authoritative over a pointer line's presence, and the authority is NON-DESTRUCTIVE.** The index says what memory *contains*: a file on disk with no pointer line is not part of memory, and a line is added or removed only by the agent, deliberately. But authority is a *reading* rule, not a licence to make one surface match the other — **removing a line never deletes the file it named**. That would be surprising and dangerous: the file is the only place the fact still lives, and a destructive edit as the silent consequence of an index edit is the one surprise a memory store must not spring.

The direction is the opposite of the 2026-07-17 hunch (which leaned file-set-authoritative), and it **closes** the three passes deferred behind the question rather than unblocking them. **Coverage** (seed a missing line from frontmatter) contradicts the rule — an unlisted file is unlisted on purpose. **Prune** (drop a bullet whose file is gone) inverts it and destroys what may be the last trace of a lost memory; under index authority the *missing file* is the anomaly, not the line. **Dedup** was already unjustified (no `merge=union` driver). What stays open is one unbuilt, non-destructive addition: a fifth compose validation naming a bullet whose target file is absent — it *reports* rather than refuses, since a dangling line does not make the composed output wrong and refusing would block every later write over a stale line.

**ONE MERGE POLICY AT EVERY LEVEL — DONE 2026-07-22 (500 green, 16 cases in `tests/tier_divergence.bats`).** David's framing, and it was the right one: there is one branch policy, two gates (pending-vs-local-`live` at `pre-commit`, local-`live`-vs-remote at `pre-push`), each of which can trigger `/gitlore:resolve` — and nesting changes nothing about it. Memory had both gates wired to `gitlore_yield_merge`; tiers had both gates in the right *places* but only *reported* at each ("resolving a tier divergence is not yet automated"), so a diverged tier silently stopped publishing while repeating the same message every session.

Almost nothing needed building. `gitlore_prepare_merge` / `gitlore_yield_merge` / `gitlore_write_merge_state` were already parameterized by store path, and **`gitlore_merge_state_file` resolves `--git-path` inside whichever store it is handed** — so every tier already got its own state file in its own gitdir, no collision. The single real blocker was the CONTINUATION: `load_continuation_state` derived its store from `gitlore_memory_path`, so it would have committed in memory while the prepared merge sat in a tier. Fixed by adding an absolute `store` field to the state file and having the continuation FIND the prepared merge by walking `gitlore_memory_stores` (memory, then each mounted tier, each guarded by `[ -e .../.git ]`). Two prepared merges is refused, not guessed at — a gate yields on the first divergence and stops, so two means the state is not what any continuation assumes. The memory-merger sub-agent reads `store` too: `changed_files` are relative to it, and it was previously free to assume memory.

Two defects surfaced while wiring it. The tier's local-`live` failure path had no parenthesized-reason discriminator, so a ref lock would have been sent into a merge that cannot fix it. And **`resolve.sh`'s two memory repairs (create missing remote, push missing `live`) each ended in `exit 0`** — which made a tier-divergence test pass green against a genuinely broken store, because the resolver repaired memory and returned before any tier was examined. Both now fall through: a repair to memory says nothing about the tiers.

**NEXT:** no slice is in flight. Open: `/gitlore:resolve` not composing, index→frontmatter sync having no keyword-density validation, and the unbuilt dangling-pointer report above.
